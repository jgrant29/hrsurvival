<% if policy(@email).show? %>
  <div class="container">

    <br><h1>Editing Email</h1>

    <%= render 'form' %>

    <%= link_to 'Show', @email %> |
    <%= link_to 'Back', emails_path %>

  </div>
<% end %>